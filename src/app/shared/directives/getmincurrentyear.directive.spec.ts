import { GetmincurrentyearDirective } from './getmincurrentyear.directive';

describe('GetmincurrentyearDirective', () => {
  it('should create an instance', () => {
    const directive = new GetmincurrentyearDirective();
    expect(directive).toBeTruthy();
  });
});
