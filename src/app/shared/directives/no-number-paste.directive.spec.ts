import { NoNumberPasteDirective } from './no-number-paste.directive';

describe('NoNumberPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new NoNumberPasteDirective();
    expect(directive).toBeTruthy();
  });
});
