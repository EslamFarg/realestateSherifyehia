import { OnlyArabicDirective } from './only-arabic.directive';

describe('OnlyArabicDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyArabicDirective();
    expect(directive).toBeTruthy();
  });
});
