import { NoStringPasteDirective } from './no-string-paste.directive';

describe('NoStringPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new NoStringPasteDirective();
    expect(directive).toBeTruthy();
  });
});
